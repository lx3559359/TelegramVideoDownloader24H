# Windows 验收记录

测试时间：2026-08-25 14:52:51 +08:00

环境：Windows，Python 3.12.10
分支：`codex/windows-background-launch-hotfix`

本记录不包含手机号、群组/频道名称、会话 ID、消息内容、API 凭据或登录会话。

## 已通过的自动化验收

- `scripts/check.ps1` 退出码为 0。
- 完整测试套件：95 项通过。
- `src` 语法编译通过。
- `pip check` 输出 `No broken requirements found.`；二维码依赖为 `qrcode 8.2`。
- 项目路径守卫确认 `.runtime`、`.cache`、`.tmp`、`logs`、`downloads` 均位于项目根目录。
- PowerShell 静态检查确认启动、守护和验证脚本均设置项目内 `TEMP`、`TMP`、`PIP_CACHE_DIR`、`PYTHONPYCACHEPREFIX`，并且不包含计划任务、用户 Startup、`AppData`、`USERPROFILE` 或硬编码 C 盘路径。
- 无凭据场景下，Tkinter 隐藏窗口冒烟检查成功创建“账号”“群组/频道”“运行”三个页面；二维码和二步验证区、手机号备用区均按预期默认隐藏，备用区可展开，随后正常关闭；初始化过程未构造 Telegram 网关或发起网络请求。
- 二维码登录自动化测试覆盖已有会话复用、生成与刷新、过期自动刷新、服务端限流等待、二步验证、错误密码重试、取消、退出账号、关闭清理和异步代次隔离。
- 配置器启动时会异步检查项目内会话；使用当前真实授权会话冒烟验证后直接显示“登录成功”、退出账号按钮可见且未生成二维码。
- Telegram 对话发现同时接受 `is_group` 和 `is_channel`，因此普通群、超级群和广播频道均可进入白名单，私聊仍被排除；当前账号只统计不记录名称时可选择 131 个群组/频道。
- 二维码通过 Tkinter Canvas 直接绘制；静态检查确认运行代码不使用 `tempfile`、Pillow、`qrcode.make` 或二维码图片文件。
- 二维码密码错误提示在清空控件前完成脱敏，专项测试确认异常文本不会显示原密码；二维码探针令牌也不会写入自检 JSON。
- 双击启动链路实际创建了标题为“Telegram 视频自动下载器”的可见窗口；后台守护进程仍保持隐藏。
- Windows 后台启动回归覆盖不兼容的 `DETACHED_PROCESS`、启动器提前退出、陈旧心跳隔离和慢启动容忍；真实 GUI 点击后立即显示 `starting`，随后生成项目内 PID、锁和 `running` 心跳，关闭 GUI 后后台继续运行。
- 端到端故障注入通过：严格白名单、历史游标重启续接、实时任务优先、临时错误重试、群组白名单热切换、停用后重新启用的增量补抓、原子文件落盘和项目内路径隔离。
- 周期增量补抓、单群访问错误退避与成功复位、当前下载文件心跳、启动配置错误、心跳过期判定、永久失败和最近错误展示均通过自动化测试。
- 自检覆盖项目路径、Python、依赖、内存二维码组件、未清理登录任务、配置、凭据格式、磁盘、SQLite、心跳与停止标记一致性和 Telegram 群组/频道可见性；损坏运行目录不再中断后续检查，单项异常隔离、空手机号、会话标识隐藏、敏感值脱敏及项目内原子报告均通过测试。
- 日志脱敏、原子心跳、停止标记、认证失效状态、SQLite 崩溃恢复和单任务 `.part` 清理均通过自动化测试。

## 自检命令冒烟验证

- 在没有真实配置和凭据的隔离工作树中执行 `.venv\Scripts\python.exe -m tg_video_downloader doctor`。
- 命令按设计返回退出码 2，并报告 6 项通过、2 项警告、3 项失败；各检查继续执行，没有因缺少配置提前崩溃。
- JSON 报告成功写入当前项目工作树的 `logs/diagnostics/`，内容未包含真实手机号、API Hash、群组/频道名称、会话 ID、消息或登录会话数据。

## 空闲资源测量

测量方式：启动完整 `DownloaderService`、真实 SQLite、协调器、单下载工作器、配置监视、心跳和停止循环；Telegram 网络边界使用内存替身，保持空闲 2 秒后读取 Windows 进程树工作集。

- 服务 Python 进程工作集：51.52 MiB。
- 虚拟环境启动器、服务 Python 和隐藏控制台的进程树合计：65.26 MiB。
- 结果：低于 150 MiB 空闲目标。

该数值是无真实网络流量的空闲测量，不代表大型视频下载期间的瞬时峰值。

## 需要用户账号完成的手工验收

以下步骤需要真实 Telegram API 凭据、验证码、可能存在的二步验证密码，以及用户有权读取的测试群组和已订阅测试频道，因此未由自动化过程代填或执行：

- 使用真实个人账号扫描内嵌二维码；确认过期自动刷新、取消连接释放、已授权会话重启复用，以及项目目录中不产生二维码图片。
- 对启用二步验证的账号确认独立密码区出现、错误密码可重试、成功或关闭后控件清空。
- 展开手机号备用区完成原验证码登录流程，确认该流程仍可用。
- 登录后刷新群组/频道列表并使用搜索逐项勾选；确认零选择无法保存或启动。
- 分别选择一个测试群和一个已订阅测试频道，确认只有白名单目标的可访问历史视频写入 `downloads`。
- 在测试群发送一个普通视频，确认当前文件结束后该实时任务先于剩余历史任务下载。
- 停止并重新启动，确认已完成文件不重复、未完成历史从游标继续。
- 关闭图形界面后，在任务管理器确认后台服务继续运行，并记录真实账号空闲及下载时的工作集。
- 在真实账号流程完成后，再检查项目根之外没有本项目主动创建的依赖、缓存、会话、日志或下载内容。

手工验收时不要把验证码、二步验证密码、会话标识或登录会话文件写入本记录。
