# Windows 验收记录

测试时间：2026-08-24 22:50:35 +08:00

环境：Windows，Python 3.12.10
代码基线：`a306c6e`

本记录不包含手机号、群名、群 ID、消息内容、API 凭据或登录会话。

## 已通过的自动化验收

- `scripts/check.ps1` 退出码为 0。
- 完整测试套件：42 项通过。
- `src` 语法编译通过。
- 项目路径守卫确认 `.runtime`、`.cache`、`.tmp`、`logs`、`downloads` 均位于项目根目录。
- PowerShell 静态检查确认启动、守护和验证脚本均设置项目内 `TEMP`、`TMP`、`PIP_CACHE_DIR`、`PYTHONPYCACHEPREFIX`，并且不包含计划任务、用户 Startup 路径或硬编码 C 盘路径。
- Tkinter 隐藏窗口冒烟检查成功创建“账号”“群组”“运行”三个页面，随后正常关闭；初始化过程未构造 Telegram 网关或发起网络请求。
- 端到端故障注入通过：严格白名单、历史游标重启续接、实时任务优先、临时错误重试、群组白名单热切换、原子文件落盘和项目内路径隔离。
- 日志脱敏、原子心跳、停止标记、认证失效状态、SQLite 崩溃恢复和单任务 `.part` 清理均通过自动化测试。

## 空闲资源测量

测量方式：启动完整 `DownloaderService`、真实 SQLite、协调器、单下载工作器、配置监视、心跳和停止循环；Telegram 网络边界使用内存替身，保持空闲 2 秒后读取 Windows 进程树工作集。

- 服务 Python 进程工作集：51.52 MiB。
- 虚拟环境启动器、服务 Python 和隐藏控制台的进程树合计：65.26 MiB。
- 结果：低于 150 MiB 空闲目标。

该数值是无真实网络流量的空闲测量，不代表大型视频下载期间的瞬时峰值。

## 需要用户账号完成的手工验收

以下步骤需要真实 Telegram API 凭据、验证码、可能存在的二步验证密码和一个用户有权操作的测试群，因此未由自动化过程代填或执行：

- 完成真实个人账号登录，刷新群列表并使用搜索逐项勾选；确认零选择无法保存或启动。
- 只选择一个测试群，确认只有该群的可访问历史视频写入 `downloads`。
- 在测试群发送一个普通视频，确认当前文件结束后该实时任务先于剩余历史任务下载。
- 停止并重新启动，确认已完成文件不重复、未完成历史从游标继续。
- 关闭图形界面后，在任务管理器确认后台服务继续运行，并记录真实账号空闲及下载时的工作集。
- 在真实账号流程完成后，再检查项目根之外没有本项目主动创建的依赖、缓存、会话、日志或下载内容。

手工验收时不要把验证码、二步验证密码、群标识或会话文件写入本记录。
